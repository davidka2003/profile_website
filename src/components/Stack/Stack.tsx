import React from "react";
import styled from "styled-components";
const StyledStack = styled.div``;
interface StackProps {}

const Stack = ({}: StackProps) => {
  return <StyledStack>Stack</StyledStack>;
};

export default Stack;
